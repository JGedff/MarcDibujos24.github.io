let arrayImagenes = {
    "abc": [],
    "anime": [],
    "blanco_negro": [],
    "dragonBall": [],
    "fondos": [],
    "fusiones": [],
    "gods": [],
    "inazuma": [],
    "nft": [],
    "oc": []
};

let counter = 0;

abc.doc.forEach(img => {
    arrayImagenes.abc[counter] = img;
    counter = counter + 1;
});

counter = 0;

anime.doc.forEach(img => {
    arrayImagenes.anime[counter] = img;
    counter = counter + 1;
});

counter = 0;

blanco_negro.doc.forEach(img => {
    arrayImagenes.blanco_negro[counter] = img;
    counter = counter + 1;
});

counter = 0;

dragonBall.doc.forEach(img => {
    arrayImagenes.dragonBall[counter] = img;
    counter = counter + 1;
});

counter = 0;

fondos.doc.forEach(img => {
    arrayImagenes.fondos[counter] = img;
    counter = counter + 1;
});

counter = 0;

fusiones.doc.forEach(img => {
    arrayImagenes.fusiones[counter] = img;
    counter = counter + 1;
});

counter = 0;

gods.doc.forEach(img => {
    arrayImagenes.gods[counter] = img;
    counter = counter + 1;
});

counter = 0;

inazuma.doc.forEach(img => {
    arrayImagenes.inazuma[counter] = img;
    counter = counter + 1;
});

counter = 0;

nft.doc.forEach(img => {
    arrayImagenes.nft[counter] = img;
    counter = counter + 1;
});

counter = 0;

oc.doc.forEach(img => {
    arrayImagenes.oc[counter] = img;
    counter = counter + 1;
});