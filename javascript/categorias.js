let arrayImagenes = {
    "abc": [],
    "anime": [],
    "cabezones": [],
    "criaturas": [],
    "digital": [],
    "dragonBall": [],
    "fondos": [],
    "gods": [],
    "inazuma": [],
    "nft": [],
    "sketch": [],
    "wwe": [],
    "cn": []
};

let counter = 0;

abc.doc.forEach(img => {
    arrayImagenes.abc[counter] = img;
    counter = counter + 1;
});

counter = 0;

anime.doc.forEach(img => {
    arrayImagenes.anime[counter] = img;
    counter = counter + 1;
});

counter = 0;

cabezones.doc.forEach(img => {
    arrayImagenes.cabezones[counter] = img;
    counter = counter + 1;
});

counter = 0;

criaturas.doc.forEach(img => {
    arrayImagenes.criaturas[counter] = img;
    counter = counter + 1;
});

counter = 0;

digital.doc.forEach(img => {
    arrayImagenes.digital[counter] = img;
    counter = counter + 1;
});

counter = 0;

dragonBall.doc.forEach(img => {
    arrayImagenes.dragonBall[counter] = img;
    counter = counter + 1;
});

counter = 0;

fondos.doc.forEach(img => {
    arrayImagenes.fondos[counter] = img;
    counter = counter + 1;
});

counter = 0;

gods.doc.forEach(img => {
    arrayImagenes.gods[counter] = img;
    counter = counter + 1;
});

counter = 0;

inazuma.doc.forEach(img => {
    arrayImagenes.inazuma[counter] = img;
    counter = counter + 1;
});

counter = 0;

nft.doc.forEach(img => {
    arrayImagenes.nft[counter] = img;
    counter = counter + 1;
});

counter = 0;

sketch.doc.forEach(img => {
    arrayImagenes.sketch[counter] = img;
    counter = counter + 1;
});

counter = 0;

wwe.doc.forEach(img => {
    arrayImagenes.wwe[counter] = img;
    counter = counter + 1;
});

counter = 0;

cn.doc.forEach(img => {
    arrayImagenes.cn[counter] = img;
    counter = counter + 1;
});